package lop.results;

public def Failure class(myError: Error) => {
    public def map fun[R] => () => Failure(myError);
    public def isSuccess fun() => false;
    public def error fun() => myError;
    
    public def toRepresentation fun() =>
        "Failure"
}
