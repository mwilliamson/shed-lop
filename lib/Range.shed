module lop.range;

members {
    Range
}

def Range class(myStart: Double, myEnd: Double) => {
    members {
        start fun() => myStart,
        end fun() => myEnd,
        equals fun(other: Range) => and(
            myStart.equals(other.start()),
            myEnd.equals(other.end())
        )
    }
}
