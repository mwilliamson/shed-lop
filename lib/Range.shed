module lop.range;

members {
    Range
}

import structs;

def Range class(myStart: Double, myEnd: Double) => {
    members {
        start fun() => myStart,
        end fun() => myEnd,
        
        struct fun() => structs.create(Range, listOf(myStart, myEnd))
    }
}
