module lop.range;

members {
    Range
}

def Range class(myStart: Double, myEnd: Double) => {
    members {
        start fun() => myStart,
        end fun() => myEnd,
        equals
    }
    
    def equals fun(other: Range) => and(
        start().equals(other.start()),
        end().equals(other.end())
    )
}
